CREATE TABLE subjects(
	id SERIAL,
	name TEXT NOT NULL UNIQUE PRIMARY KEY,
	is_active BOOLEAN DEFAULT TRUE
);

INSERT 
INTO subjects(name, is_active)
VALUES ('עת"פ', DEFAULT),
	   ('OOP', DEFAULT),
	   ('DEVOPS', DEFAULT),
	   ('DB', DEFAULT),
	   ('WEB', DEFAULT);


CREATE TABLE segels(
	id SERIAL,
	name TEXT NOT NULL UNIQUE PRIMARY KEY,
	is_active BOOLEAN DEFAULT TRUE
);

INSERT 
INTO segels(name, is_active)
VALUES ('SG', DEFAULT),
	   ('SD', DEFAULT),
	   ('SH', DEFAULT),
	   ('SV', DEFAULT);